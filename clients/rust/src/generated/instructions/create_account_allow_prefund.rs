//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;

pub const CREATE_ACCOUNT_ALLOW_PREFUND_DISCRIMINATOR: u32 = 13;

/// Accounts.
#[derive(Debug)]
pub struct CreateAccountAllowPrefund {
    pub new_account: solana_address::Address,

    pub payer: Option<solana_address::Address>,
}

impl CreateAccountAllowPrefund {
    pub fn instruction(
        &self,
        args: CreateAccountAllowPrefundInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: CreateAccountAllowPrefundInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.new_account, true));
        if let Some(payer) = self.payer {
            accounts.push(solana_instruction::AccountMeta::new(payer, true));
        }
        accounts.extend_from_slice(remaining_accounts);
        let mut data = CreateAccountAllowPrefundInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::SYSTEM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct CreateAccountAllowPrefundInstructionData {
    discriminator: u32,
}

impl CreateAccountAllowPrefundInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 13 }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for CreateAccountAllowPrefundInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct CreateAccountAllowPrefundInstructionArgs {
    pub lamports: u64,
    pub space: u64,
    pub program_address: Address,
}

impl CreateAccountAllowPrefundInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `CreateAccountAllowPrefund`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` new_account
///   1. `[writable, signer, optional]` payer
#[derive(Clone, Debug, Default)]
pub struct CreateAccountAllowPrefundBuilder {
    new_account: Option<solana_address::Address>,
    payer: Option<solana_address::Address>,
    lamports: Option<u64>,
    space: Option<u64>,
    program_address: Option<Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl CreateAccountAllowPrefundBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn new_account(&mut self, new_account: solana_address::Address) -> &mut Self {
        self.new_account = Some(new_account);
        self
    }
    /// `[optional account]`
    #[inline(always)]
    pub fn payer(&mut self, payer: Option<solana_address::Address>) -> &mut Self {
        self.payer = payer;
        self
    }
    /// `[optional argument, defaults to '0']`
    #[inline(always)]
    pub fn lamports(&mut self, lamports: u64) -> &mut Self {
        self.lamports = Some(lamports);
        self
    }
    #[inline(always)]
    pub fn space(&mut self, space: u64) -> &mut Self {
        self.space = Some(space);
        self
    }
    #[inline(always)]
    pub fn program_address(&mut self, program_address: Address) -> &mut Self {
        self.program_address = Some(program_address);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = CreateAccountAllowPrefund {
            new_account: self.new_account.expect("new_account is not set"),
            payer: self.payer,
        };
        let args = CreateAccountAllowPrefundInstructionArgs {
            lamports: self.lamports.clone().unwrap_or(0),
            space: self.space.clone().expect("space is not set"),
            program_address: self
                .program_address
                .clone()
                .expect("program_address is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `create_account_allow_prefund` CPI accounts.
pub struct CreateAccountAllowPrefundCpiAccounts<'a, 'b> {
    pub new_account: &'b solana_account_info::AccountInfo<'a>,

    pub payer: Option<&'b solana_account_info::AccountInfo<'a>>,
}

/// `create_account_allow_prefund` CPI instruction.
pub struct CreateAccountAllowPrefundCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub new_account: &'b solana_account_info::AccountInfo<'a>,

    pub payer: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// The arguments for the instruction.
    pub __args: CreateAccountAllowPrefundInstructionArgs,
}

impl<'a, 'b> CreateAccountAllowPrefundCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: CreateAccountAllowPrefundCpiAccounts<'a, 'b>,
        args: CreateAccountAllowPrefundInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            new_account: accounts.new_account,
            payer: accounts.payer,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.new_account.key,
            true,
        ));
        if let Some(payer) = self.payer {
            accounts.push(solana_instruction::AccountMeta::new(*payer.key, true));
        }
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = CreateAccountAllowPrefundInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::SYSTEM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.new_account.clone());
        if let Some(payer) = self.payer {
            account_infos.push(payer.clone());
        }
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `CreateAccountAllowPrefund` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` new_account
///   1. `[writable, signer, optional]` payer
#[derive(Clone, Debug)]
pub struct CreateAccountAllowPrefundCpiBuilder<'a, 'b> {
    instruction: Box<CreateAccountAllowPrefundCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> CreateAccountAllowPrefundCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(CreateAccountAllowPrefundCpiBuilderInstruction {
            __program: program,
            new_account: None,
            payer: None,
            lamports: None,
            space: None,
            program_address: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn new_account(
        &mut self,
        new_account: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.new_account = Some(new_account);
        self
    }
    /// `[optional account]`
    #[inline(always)]
    pub fn payer(&mut self, payer: Option<&'b solana_account_info::AccountInfo<'a>>) -> &mut Self {
        self.instruction.payer = payer;
        self
    }
    /// `[optional argument, defaults to '0']`
    #[inline(always)]
    pub fn lamports(&mut self, lamports: u64) -> &mut Self {
        self.instruction.lamports = Some(lamports);
        self
    }
    #[inline(always)]
    pub fn space(&mut self, space: u64) -> &mut Self {
        self.instruction.space = Some(space);
        self
    }
    #[inline(always)]
    pub fn program_address(&mut self, program_address: Address) -> &mut Self {
        self.instruction.program_address = Some(program_address);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = CreateAccountAllowPrefundInstructionArgs {
            lamports: self.instruction.lamports.clone().unwrap_or(0),
            space: self.instruction.space.clone().expect("space is not set"),
            program_address: self
                .instruction
                .program_address
                .clone()
                .expect("program_address is not set"),
        };
        let instruction = CreateAccountAllowPrefundCpi {
            __program: self.instruction.__program,

            new_account: self
                .instruction
                .new_account
                .expect("new_account is not set"),

            payer: self.instruction.payer,
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct CreateAccountAllowPrefundCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    new_account: Option<&'b solana_account_info::AccountInfo<'a>>,
    payer: Option<&'b solana_account_info::AccountInfo<'a>>,
    lamports: Option<u64>,
    space: Option<u64>,
    program_address: Option<Address>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
